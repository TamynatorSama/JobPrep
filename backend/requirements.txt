fastapi>=0.115.0
uvicorn[standard]>=0.32.0
sse-starlette>=2.1.0
langchain-core>=0.3.0
langchain-google-genai>=2.0.0
langgraph>=0.2.0
python-dotenv>=1.0.0
playwright>=1.49.0
python-docx>=1.1.2
# After pip install, run: playwright install chromium

# Voice (live mock interview) is optional + heavy (torch + models). It lives in
# requirements-voice.txt — install with:  .\setup.ps1 -Voice
